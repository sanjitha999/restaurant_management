name: resturant_management
description: App for managing restaurants and orders
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  bloc: ^8.1.4
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  flutter_bloc: ^8.1.6
  http: ^1.2.2
  lint: ^2.3.0
  intl_utils: ^2.8.7
  json_annotation: ^4.9.0
  flutter_simple_dependency_injection: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.2
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
